# 实际体验与需求池

本文持续记录系统所有者在真实使用中的体验、问题和需求，是后续版本规划的权威用户输入之一，但不直接替代具体任务文档。

## 使用方式

- 系统所有者可以直接编辑本文，也可以在对话中把临时需求告诉 Codex。
- 当系统所有者明确要求“根据需求池规划并执行”时，Codex 应先阅读本文、合并重复项、识别依赖与口径影响，再建立或更新对应任务文档。
- 临时直接提出的需求可以立即进入任务分析；如有持续跟踪价值，再保留到本文。
- 每次只实施一个任务文档定义的范围。会改变统计口径、账期归属、分期入账、备份格式或安全边界的需求，必须先完成需求或设计变更。
- 不在本文记录真实金额、密码、密钥、Cookie、完整账单行或其他敏感财务数据；示例必须脱敏。

## 状态说明

| 状态 | 含义 |
|---|---|
| 待梳理 | 已记录，尚未分析范围与优先级 |
| 待确认 | 需要系统所有者补充场景、目标或口径 |
| 已规划 | 已转换为具体任务文档 |
| 实施中 | 对应任务正在开发或验证 |
| 已完成 | 已上线并完成对应验收 |
| 暂不采用 | 当前版本不实施，并保留原因 |

## 记录模板

复制以下小节填写；不了解的字段可以留空。

```markdown
### YYYY-MM-DD 简短标题

- 状态：待梳理
- 使用场景：
- 当前体验或问题：
- 期望结果：
- 发生频率：偶发 / 经常 / 每次
- 影响程度：低 / 中 / 高
- 当前临时处理方式：
- 脱敏示例或补充说明：
```

## 待梳理记录

<!-- 在此处按时间追加实际体验和需求，保留系统所有者的原始意图。 -->

## 已规划或已完成记录

<!-- 转换为任务后，在原记录中补充任务链接和状态，不删除历史记录。 -->

### 2026-08-05 页面控件重复显示

- 状态：已完成
- 使用场景：生产首页使用 Aurora Ledger 主题时。
- 当前体验或问题：基础结构样式未生效时，桌面导航与移动导航同时显示，形成重复控件。
- 期望结果：页面在样式加载异常时也不显示两套导航，并建立可重复执行的低风险快速迭代模式。
- 发生频率：偶发
- 影响程度：中
- 当前临时处理方式：刷新页面以重新加载基础样式。
- 脱敏示例或补充说明：见 `demands/屏幕截图 2026-08-05 013653.png`；[`QUICK-ITERATION-01`](quick-iteration-01-navigation-fallback.md) 已通过生产快速发布验收。

### 2026-08-15 多个页面状态文案和组件不一致

- 状态：已完成
- 使用场景：信用卡概览、预算管理、风险预测、分期预览、首页预警等页面。
- 当前体验或问题：页面直接显示 `OVERDUE`、`SAFE`、`OVER`、`DANGER` 等内部枚举；零值使用比例和正式账单金额被显示为“—”；部分表单和表格组件不统一。
- 期望结果：状态使用中文标签和统一状态徽章，有效零值正常显示，表单和表格遵循统一组件契约。
- 发生频率：每次
- 影响程度：中
- 当前临时处理方式：无
- 脱敏示例或补充说明：修复提交 `22696a7`；发布提交 `e638dbc`；验收见 `docs/acceptance.md` 和 `docs/maintenance-history.md`。

### 2026-08-15 主题跨文件系统发布失败

- 状态：已完成
- 使用场景：`THEME_IMPORT_TMP_DIR` 与 `THEME_RUNTIME_DIR` 不在同一文件系统时导入主题。
- 当前体验或问题：`os.replace` 返回 `EXDEV`，主题导入失败。
- 期望结果：跨文件系统时仍可安全发布，且失败不留部分主题目录。
- 发生频率：按部署拓扑可能出现
- 影响程度：高
- 当前临时处理方式：无
- 脱敏示例或补充说明：修复提交 `e638dbc`；验收见 `docs/maintenance-history.md`。

### 2026-08-15 收入/支出表单辨识度不足且页面间距拥挤

- 状态：已完成
- 使用场景：手工交易录入、预算管理等页面。
- 当前体验或问题：“记收入”和“记支出”页面几乎没有区别；预算按钮拥挤，文字与边框过近，其他页面也有类似间距问题。
- 期望结果：交易类型有明确徽章、说明和差异化提交按钮；全局控件间距统一。
- 发生频率：每次
- 影响程度：中
- 当前临时处理方式：无
- 脱敏示例或补充说明：修复提交 `5e0d740`；验收见 `docs/acceptance.md` 和 `docs/maintenance-history.md`。

### 2026-08-15 收入/支出标签共用

- 状态：已完成
- 使用场景：记收入时选择交易标签。
- 当前体验或问题：收入分类和支出分类独立，但标签全局共用，“记收入”可以标记为“冲动消费”。
- 期望结果：标签按收入/支出类型隔离，表单和服务层双重校验。
- 发生频率：每次
- 影响程度：中
- 当前临时处理方式：无
- 脱敏示例或补充说明：提交 `294c974`，迁移 `ledger.0006_tag_applies_to`；验收见 `docs/acceptance.md` 和 `docs/maintenance-history.md`。

### 2026-08-15 缺少标签管理入口

- 状态：已完成
- 使用场景：需要新建、编辑或停用交易标签。
- 当前体验或问题：系统只有默认“冲动消费”标签，没有标签管理页面。
- 期望结果：提供标签管理页面，支持按类型新建、编辑、启停，已使用标签禁止直接删除。
- 发生频率：每次
- 影响程度：中
- 当前临时处理方式：直接操作数据库或测试代码创建，不推荐。
- 脱敏示例或补充说明：提交 `9e14d2e`；入口“分析与管理 → 标签管理”；验收见 `docs/acceptance.md` 和 `docs/maintenance-history.md`。

### 2026-08-15 分类预算入口不清晰、分类缺少删除、计划分类混用

- 状态：已完成
- 使用场景：预算管理、分类管理、固定支出与预计收入创建。
- 当前体验或问题：预算管理主要暴露月度总预算，分类预算入口不明显；分类没有删除按钮；创建计划时分类下拉同时显示收入和支出分类。
- 期望结果：可批量设置分类预算；未使用分类可删除、已使用分类保护；计划分类按方向过滤。
- 发生频率：每次
- 影响程度：中
- 当前临时处理方式：无
- 脱敏示例或补充说明：提交 `5a076c8`；验收见 `docs/acceptance.md` 和 `docs/maintenance-history.md`。
